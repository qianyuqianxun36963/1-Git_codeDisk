这里的hibernate.cfg.xml

两种用法

一、可以将“它所在的目录”加入classpath，然后在代码中：

Configuration configuration = new Configuration().configure();
            sessionFactory = configuration.buildSessionFactory();

可以直接使用。位置有点死。

二、比较推荐，可以在配置spring Bean 的时候指定位置，而且位置很随意。